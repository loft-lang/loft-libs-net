[package]
name = "server"
version = "0.1.2"
loft = ">=0.8"
description = "HTTP + WebSocket server — TCP sockets + tungstenite via cdylib."

[library]
entry = "src/server.loft"
native = "loft_server"

[native]
crate = "loft-server"

[dependencies]
web = ">=0.1"
