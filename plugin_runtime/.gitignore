# Build artifacts — regenerated by build/sign-and-build-fixtures.sh + cargo.
**/target/
**/Cargo.lock

# The committed test fixtures (build/counter_plugin.wasm, *.sig) and the
# test-only keys/ ARE checked in so the security-gate tests verify in CI
# without a key-generation step — they are NOT ignored.

# loft interpreter cache (from running counter_demo.loft)
**/.loft/
